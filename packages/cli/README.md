# @tlog/cli

CLI for TLog, a YAML-first test management tool.

## Install

```bash
npm install -g @tlog/cli
```

## Usage

```bash
tlog --help
tlog init
```

`tlog init` creates `tests/index.yaml` in the current directory.

## Current Status

This package is in early development. Additional commands will be added.
