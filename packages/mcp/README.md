# @tlog/mcp

MCP server package for TLog.

## Run

```bash
npx @tlog/mcp --stdio
```

## Protocol

This package currently supports a minimal MCP/JSON-RPC handshake:

- `initialize`: returns server info and capabilities

## Current Status

This package is in early development. MCP tools and resources will be added.
